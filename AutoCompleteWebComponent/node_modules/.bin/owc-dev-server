#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../owc-dev-server/owc-dev-server.js" "$@"
  ret=$?
else 
  node  "$basedir/../owc-dev-server/owc-dev-server.js" "$@"
  ret=$?
fi
exit $ret
